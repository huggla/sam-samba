#!/bin/sh
set -e

echo 'Defaults lecture="never"' > /etc/sudoers.d/samba
echo "samba $(hostname)=(root) NOPASSWD: /usr/sbin/nmbd,/usr/sbin/smbd" >> /etc/sudoers.d/samba
/bin/rm -r `dirname $0`
exit 0
