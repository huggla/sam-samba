#!/usr/local/bin/dash
set -e +a +m +s +i -f

setSmbpasswdForUserFromFile(){
   local user="$1"
   local pwFile="$2"
   local userPw=""
   read userPw < "$pwFile"
   echo "$userPw" | /usr/local/bin/smbpasswd -s -a "$user"
}

setSmbpasswdForUserFromFile "$1" "$2"
